# Інструкція з налаштування Slot Booking MVP

## Крок 1: Встановлення залежностей

```bash
npm install
```

## Крок 2: Налаштування Supabase (локально)

### 2.1. Встановіть Docker Desktop

Supabase потребує Docker для локального запуску:
- macOS: https://docs.docker.com/desktop/install/mac-install/
- Windows: https://docs.docker.com/desktop/install/windows-install/
- Linux: https://docs.docker.com/desktop/install/linux-install/

### 2.2. Запустіть локальний Supabase

```bash
npm run supabase:start
```

При першому запуску це займе кілька хвилин для завантаження контейнерів.

### 2.3. Отримайте credentials

Після успішного запуску ви побачите вивід з credentials:

```
API URL: http://127.0.0.1:54321
anon key: eyJhbGc...
service_role key: eyJhbGc...
```

### 2.4. Застосуйте міграції

Міграції будуть застосовані автоматично при запуску. Перевірте статус:

```bash
npm run supabase:status
```

## Крок 3: Налаштування Telegram Bot

### 3.1. Створіть бота

1. Напишіть [@BotFather](https://t.me/BotFather) в Telegram
2. Відправте команду `/newbot`
3. Слідуйте інструкціям для створення бота
4. Збережіть токен, який ви отримаєте (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3.2. Налаштуйте бота для логіну

1. Відправте `/setdomain` в BotFather
2. Виберіть вашого бота
3. Введіть домен: `localhost` (для локальної розробки)

### 3.3. Отримайте Bot Username

Ваш bot username буде у форматі `@your_bot_name_bot` (без @)

### 3.4. Отримайте Bot ID

Bot ID - це числа на початку токена (до двокрапки).
Наприклад, якщо токен `1234567890:ABC...`, то ID = `1234567890`

## Крок 4: Налаштування Environment змінних

Створіть файл `.env.local` в корені проекту:

```env
# Supabase (отримали на кроці 2.3)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-supabase
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-supabase

# Telegram Bot (отримали на кроці 3)
TELEGRAM_BOT_TOKEN=your-bot-token-here
TELEGRAM_BOT_USERNAME=your_bot_username
NEXT_PUBLIC_TELEGRAM_BOT_ID=your-bot-id

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**Важливо:** Замініть всі `your-*` значення на реальні!

## Крок 5: Запуск додатку

```bash
npm run dev
```

Відкрийте браузер: [http://localhost:3000](http://localhost:3000)

## Крок 6: Перевірка функціоналу

### 6.1. Авторизація

1. Відкрийте http://localhost:3000/login
2. Натисніть на кнопку "Login with Telegram"
3. Авторизуйтесь через Telegram

### 6.2. Створення збору

1. На головній сторінці натисніть "Створити збір"
2. Заповніть форму:
   - Назва: "Тестовий збір"
   - Опис: "Перевірка функціоналу"
   - Дата: виберіть завтрашній день
   - Кількість місць: 5
3. Натисніть "Створити збір"

### 6.3. Бронювання місця

1. Відкрийте створений збір
2. Натисніть "Зайняти місце"
3. Перевірте що ваше ім'я з'явилось в списку учасників

### 6.4. Черга очікування

1. Створіть збір з 2 місцями
2. Забронюйте місце (авторизуйтесь як перший користувач)
3. Відкрийте збір в інкогніто/приватному вікні
4. Авторизуйтесь як другий користувач та забронюйте
5. Відкрийте збір в третьому вікні
6. Авторизуйтесь як третій користувач
7. Натисніть "Встати в чергу"
8. Перевірте що ви в черзі
9. В першому вікні (перший користувач) звільніть місце
10. Перевірте що третій користувач автоматично отримав місце

## Корисні команди

### Supabase

```bash
# Запустити Supabase
npm run supabase:start

# Зупинити Supabase
npm run supabase:stop

# Перезапустити БД (видалить всі дані!)
npm run supabase:reset

# Статус Supabase
npm run supabase:status
```

### Next.js

```bash
# Розробка
npm run dev

# Збірка для продакшн
npm run build

# Запуск продакшн версії
npm start

# Перевірка коду
npm run lint
```

## Доступ до Supabase Studio

Supabase Studio (UI для управління БД) доступний за адресою:
http://127.0.0.1:54323

Тут ви можете:
- Переглядати таблиці
- Виконувати SQL запити
- Переглядати логи
- Налаштовувати RLS політики

## Troubleshooting

### Помилка: "Cannot connect to Supabase"

1. Перевірте чи запущений Docker Desktop
2. Перевірте чи запущений Supabase: `npm run supabase:status`
3. Перезапустіть Supabase: `npm run supabase:stop && npm run supabase:start`

### Помилка: "Telegram bot не налаштований"

1. Перевірте файл `.env.local`
2. Переконайтесь що `TELEGRAM_BOT_TOKEN` має правильне значення
3. Перезапустіть Next.js сервер

### Помилка: "Invalid Telegram auth"

1. Перевірте що ви налаштували домен в BotFather
2. Для локальної розробки домен має бути `localhost`
3. Перевірте що `NEXT_PUBLIC_TELEGRAM_BOT_ID` правильний

### Міграції не застосувались

```bash
npm run supabase:reset
```

Це видалить всі дані та заново застосує міграції.

## Production Deploy

Для деплою на Vercel та Supabase Cloud дивіться [DEPLOYMENT.md](./DEPLOYMENT.md) (буде додано пізніше)

## Структура проекту

```
/app
  /(auth)              # Сторінки авторизації
    /login             # Telegram Login
  /(dashboard)         # Основні сторінки
    /page.tsx          # Головна - список сборів
    /create            # Створення збору
    /gathering/[id]    # Сторінка конкретного збору
    /my                # Мої збори
  /api                 # API routes
    /auth/telegram     # Telegram auth callback
    /gatherings        # CRUD операції зі сборами
    /telegram          # Telegram bot webhook та notifications
/components
  /ui                  # shadcn/ui компоненти
  /gathering           # Компоненти для сборів
  /layout              # Header, navigation
/lib
  /supabase           # Supabase клієнти та типи
  /telegram           # Telegram bot логіка
  /actions            # Server actions
  /utils              # Утиліти та валідація
/supabase
  /migrations         # SQL міграції
```

## Наступні кроки

Після успішного запуску локально ви можете:

1. ✅ Протестувати всі функції
2. 🔄 Налаштувати Telegram уведомлення в групу (потребує реального бота)
3. 🚀 Задеплоїти на Vercel + Supabase Cloud
4. 📱 Налаштувати webhook для Telegram бота
5. ✨ Додати додаткові features з v2

## Допомога

Якщо виникли проблеми:
1. Перевірте всі кроки ще раз
2. Подивіться логи в консолі браузера
3. Подивіться логи Next.js в терміналі
4. Перевірте логи Supabase в Studio

Приємного використання! 🎮

